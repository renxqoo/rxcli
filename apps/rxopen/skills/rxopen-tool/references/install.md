<!-- 此文件由 build 自动生成，不要手改 -->

# 安装 rxopen

仅在 `rxopen` 命令不可用时读取本指引。

安装要求:Node.js >=20。先告知用户下面的命令会全局安装 CLI 并同步整套 rxopen skills；获得用户同意后再执行:

```bash
npx @renxqoo/rxopen-cli install
```

安装后验证:

```bash
rxopen --help
```

安装失败时返回原始错误和可执行的修复建议，不要循环重试。
